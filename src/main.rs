use crate::game_container::{GameContainer, GAME_ROWS};
use crate::tile::{Tile, Coordinate};
use std::{io::stdin, ops::Add};

mod tests;
mod game_container;
mod tile;




fn main() {
    let mut game = GameContainer::new("testt");

    
    
    
}
